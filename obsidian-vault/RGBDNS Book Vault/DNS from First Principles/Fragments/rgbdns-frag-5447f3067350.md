---
type: "code-fragment"
fragment_id: "rgbdns-frag-5447f3067350"
source_path: "src/rbl.rs"
code_note: "DNS from First Principles/Code/src/rbl.rs.source"
language: "rust"
subsystem: "Specialized responders"
crate: "rgbdns"
symbol: "cdb_roundtrip_preserves_rbl_answers"
kind: "fn"
start_line: 269
end_line: 286
---

# cdb_roundtrip_preserves_rbl_answers

- Fragment ID: `rgbdns-frag-5447f3067350`
- Source file: [[DNS from First Principles/Code/src/rbl.rs.source|src/rbl.rs]]
- Lines: 269-286
- Subsystem: [[DNS from First Principles/Subsystems/Specialized responders|Specialized responders]]
- Component: [[DNS from First Principles/Components/rgbdns|rgbdns]]

```rgbdns-fragment
{"id": "rgbdns-frag-5447f3067350", "codeNote": "DNS from First Principles/Code/src/rbl.rs.source", "heading": "rgbdns-frag-5447f3067350: fn cdb_roundtrip_preserves_rbl_answers", "sourcePath": "src/rbl.rs", "startLine": 269, "endLine": 286}
```

## Excerpt

<span id="rgbdns-frag-5447f3067350" class="rgbdns-fragment-target"></span>
### rgbdns-frag-5447f3067350: fn cdb_roundtrip_preserves_rbl_answers

```rust
    fn cdb_roundtrip_preserves_rbl_answers() {
        let database = Database::parse(":127.0.0.3:Listed\n192.0.2/24\n").unwrap();
        let path = std::env::temp_dir().join(format!(
            "rgbdns-rbl-{}-{}.cdb",
            std::process::id(),
            SystemTime::now()
                .duration_since(SystemTime::UNIX_EPOCH)
                .unwrap()
                .as_nanos()
        ));
        compile(&database, &path).unwrap();
        let loaded = Database::from_file(&path).unwrap();
        fs::remove_file(path).unwrap();
        assert!(loaded.listed(Ipv4Addr::new(192, 0, 2, 44)));
        assert!(!loaded.listed(Ipv4Addr::new(192, 0, 3, 44)));
        assert_eq!(loaded.response().0, Ipv4Addr::new(127, 0, 0, 3));
    }
}
```
