# rgbdns ACME DNS-01 update policy. Updates remain disabled until
# ACME_UPDATE_CONFIG points to this file and at least one policy is configured.
#
# key-name algorithm base64-secret zone owner-prefix ttl
# certbot-example. hmac-sha256. REPLACE_WITH_BASE64_SECRET example.org. _acme-challenge. 60
